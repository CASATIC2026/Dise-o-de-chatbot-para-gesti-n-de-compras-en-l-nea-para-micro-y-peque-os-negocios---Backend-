services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: chatbot-db
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - chatbot-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Inventory Microservice (.NET 8)
  inventario-service:
    build:
      context: ./src
      dockerfile: Services.Inventario/Dockerfile.dev
    container_name: inventario-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      # CAMBIO: Host=postgres para coincidir con el nombre del servicio arriba
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION=${JWT_EXPIRATION}
    volumes:
      - ./src/Services.Inventario:/src/Services.Inventario
      - ./src/Shared.Core:/src/Shared.Core
    ports:
      - "5001:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - chatbot-network

  # Node.js Gateway & Chatbot
  # Payments Microservice (.NET 8)
  pagos-service:
    build:
      context: ./src
      dockerfile: Services.Pagos/Dockerfile.dev
    container_name: pagos-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}
      - Wompi__PublicKey=${WOMPI_PUBLIC_KEY}
      - Wompi__PrivateKey=${WOMPI_PRIVATE_KEY}
      - Wompi__WebhookSecret=${WOMPI_WEBHOOK_SECRET}
    volumes:
      - ./src/Services.Pagos:/src/Services.Pagos
      - ./src/Shared.Core:/src/Shared.Core
    ports:
      - "5002:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - chatbot-network

  # Chatbot Microservice (.NET 8)
  chatbot-service:
    build:
      context: ./src
      dockerfile: Services.Chatbot/Dockerfile.dev
    container_name: chatbot-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ./src/Services.Chatbot:/src/Services.ChatBot
      - ./src/Shared.Core:/src/Shared.Core
    ports:
      - "5003:8080"
    networks:
      - chatbot-network

  # Node.js Gateway
  gateway:
    build:
      context: ./src/Gateway.NodeJS
      dockerfile: Dockerfile.dev
    container_name: gateway-service
    environment:
      - NODE_ENV=${NODE_ENV}
      - PORT=${GATEWAY_PORT}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION=${JWT_EXPIRATION}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_WEBHOOK_URL=${TELEGRAM_WEBHOOK_URL}
      - INVENTARIO_SERVICE_URL=http://inventario-service:8080
      - PAGOS_SERVICE_URL=http://pagos-service:8080
      - CHATBOT_SERVICE_URL=http://chatbot-service:8080
    volumes:
      - ./src/Gateway.NodeJS:/app
      - /app/node_modules
    ports:
      - "${GATEWAY_PORT}:${GATEWAY_PORT}"
    depends_on:
      - postgres
      - inventario-service
    networks:
      - chatbot-network

  # Admin Web Panel (React)
  admin-web:
    build:
      context: ./src/Admin.Web
      dockerfile: Dockerfile.dev
    container_name: admin-web
    environment:
      - VITE_API_URL=http://localhost:${GATEWAY_PORT}
    volumes:
      - ./src/Admin.Web:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - gateway
    networks:
      - chatbot-network

networks:
  chatbot-network:
    driver: bridge

volumes:
  postgres_data:
    name: postgres_data
